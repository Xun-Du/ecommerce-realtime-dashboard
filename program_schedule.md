# 电商增长实验与营销归因平台：产品与工程开发计划

> **产品定位**：面向品牌电商增长团队的 Commerce Growth Intelligence & Experimentation Platform。
> **核心闭环**：发现经营变化 → 定位渠道与漏斗原因 → 运行实验验证策略 → 形成上线或预算决策。
> **当前状态**：已完成可运行的 M4 基线，包括 Supabase Postgres 数据底座、14 天模拟事件流、FastAPI 指标/漏斗/单实验评估 API，以及 Streamlit 经营看板。归因、实验管理、预警和真实平台接入尚未实现。
> **当前技术路线**：Supabase Postgres + FastAPI + Streamlit + Plotly；MVP 继续使用模拟数据完成产品闭环，并为 Shopify、广告平台和埋点 SDK 预留接口。

---

## 1. 产品目标与边界

### 1.1 产品命名

- **中文类别**：电商增长实验与营销归因平台
- **英文类别**：Commerce Growth Intelligence & Experimentation Platform
- **项目工作名**：E-commerce Growth Intelligence
- **一句话价值**：用统一的经营指标、透明的营销归因和可信的 A/B 实验，帮助品牌电商团队更快判断“哪里出了问题、增长来自哪里、策略是否有效”。

“数据看板”只是平台的展示层，不再作为产品类别。平台的核心价值是把监测、诊断、验证和决策连接成闭环。

### 1.2 首要用户

| 用户角色 | 核心问题 | 主要使用模块 | 预期决策 |
| --- | --- | --- | --- |
| 增长产品经理 | 新页面、新推荐或新优惠是否有效 | 指标监测、实验中心 | 继续观察、停止、扩大流量或全量上线 |
| 电商运营 | GMV/转化为何波动，漏斗哪里流失 | 总览、趋势、漏斗 | 调整页面、活动、商品或运营节奏 |
| 投放/渠道运营 | 哪个渠道真正带来订单和收入 | 归因分析 | 调整渠道预算和活动组合 |
| 数据产品经理/分析师 | 指标口径和实验结论是否可信 | 全模块、数据质量 | 统一口径、排查异常、复核结论 |

### 1.3 典型使用场景

1. **日常经营监控**：发现 GMV 或购买转化率异常，通过趋势与漏斗定位变化环节。
2. **渠道预算优化**：比较首次触达、末次触达和线性归因结果，识别拉新渠道与促转渠道。
3. **活动复盘**：拆解活动期间的渠道、商品、人群和转化表现，区分自然增长、投放贡献和实验增量。
4. **策略实验**：围绕首页、商品排序、推荐、优惠和结算流程创建实验并监控结果。
5. **上线决策**：结合样本量、uplift、置信区间、显著性、GMV 和护栏指标给出业务建议。
6. **异常止损**：实验或活动导致关键指标恶化时，及时暂停并保留复盘证据。

### 1.4 MVP 范围

**范围内：**

- 单项目、单店铺视角的经营监测。
- 模拟事件数据与准实时刷新。
- 规则型归因：首次触达、末次触达、线性归因。
- 多实验列表与单实验详情；A/B 两组实验为主。
- 可解释的统计结果、业务结论和数据质量提示。
- 桌面端演示体验、本地运行、自动化测试和可选云部署。

**暂不包含：**

- 真实支付、退款、税费和财务对账。
- 多租户、细粒度权限、审批流和审计后台。
- 自动执行广告预算调整或实验发布。
- 秒级流计算、跨设备身份合并和生产级高可用。
- 因果归因、Markov 等高级模型；待规则模型口径稳定后再评估。

---

## 2. 产品信息架构与前端壳层

```text
┌──────────────────────┬──────────────────────────────────────────────┐
│ 品牌 / 店铺           │ 页面标题                         数据状态      │
│                      ├──────────────────────────────────────────────┤
│ Home                 │ 页面级筛选：时间 / 店铺 / 数据源 / 刷新       │
│ Monitor              │                                              │
│ Attribution          │ 右侧工作区：指标卡、图表、表格、下钻面板       │
│ Funnel               │                                              │
│ Customers            │                                              │
│ Experiments          │                                              │
│ Creatives            │                                              │
│ Integrations         │                                              │
└──────────────────────┴──────────────────────────────────────────────┘
```

### 2.1 参考图对应的界面结构

前端采用“固定左侧导航 + 右侧模块工作区”的应用壳层。左侧导航始终存在，点击后只替换右侧内容，不使用多个互相独立的站点或弹窗堆叠页面。整体视觉参考用户提供的 Ordinary 截图：深色背景、细边框卡片、低饱和灰色辅助文字和单一亮色强调线。

| 区域 | 规范 | 实现要求 |
| --- | --- | --- |
| 左侧导航 | 深色垂直栏，桌面端宽度约 168–196px；当前项使用浅色背景和左侧亮色指示条 | 使用统一 `NavigationState` 保存当前模块；刷新后默认回到 Home |
| 品牌区 | 顶部显示产品名/店铺名，底部预留用户与设置入口 | 不显示数据库密钥或内部服务地址 |
| 右侧顶部 | 显示模块标题、更新时间、数据源状态和“立即刷新” | 页面级筛选置于标题下方；筛选变更只刷新右侧数据区域 |
| 内容区 | 使用 12 列栅格；指标卡、趋势图、表格和诊断卡均为独立容器 | 单个 API 失败只影响对应容器，并显示可读错误 |
| 指标卡 | 4 个为一组；标签小字号大写/灰色，数值突出，底部显示相对变化 | 金额、数量、比例统一格式；无基准或分母为 0 显示 `—` |
| 图表卡 | 卡片标题左对齐，右上角显示口径/数据源备注；图表不与页面背景融为一体 | Plotly 统一深色模板，隐藏非必要网格线和边框 |
| 强调色 | 使用品牌粉/洋红作为主趋势线和选中态；绿色仅表示正向变化，红色仅表示风险 | 不能用颜色单独传递统计结论，必须同时显示文字 |
| 空/错误状态 | 在容器内部展示“暂无数据”“样本不足”“数据质量异常”及下一步动作 | 禁止以空白页面、0 或成功色替代状态 |

### 2.2 导航与模块映射

导航命名保留截图中的产品感，同时映射到本项目的业务能力：

| 导航项 | 优先级 | 页面职责 | 首版内容 |
| --- | --- | --- | --- |
| **Home** | P0 | 经营总览和快捷决策入口 | Revenue/GMV、Orders、True ROAS（无成本数据时显示未接入）、New Customers、趋势、异常摘要 |
| **Monitor** | P0 | 数据指标监测 | 指标趋势、指标对比、维度筛选、阈值状态 |
| **Attribution** | P1 | 营销归因 | 首次触达、末次触达、线性归因、渠道贡献和下钻 |
| **Funnel** | P0 | 转化路径诊断 | Click → Add to cart → Buy，步骤转化、流失和质量标记 |
| **Customers** | P1 | 客户与人群分析 | 新老客、复购、设备/地区/渠道分群；MVP 先提供占位导航和数据边界说明 |
| **Experiments** | P1 | 实验列表、配置和结果 | 实验状态、主指标、uplift、显著性、护栏和结论 |
| **Creatives** | P2 | 素材/活动表现 | 活动或素材维度的曝光、点击、订单、GMV；依赖 campaign 数据 |
| **Integrations** | P2 | 数据源与同步状态 | Shopify、广告平台、埋点 SDK 的连接状态、更新时间和错误 |

Home 是默认入口，但不承担所有明细分析。它只展示关键指标和摘要，并通过“查看详情”将用户引导到 Monitor、Attribution、Funnel 或 Experiments。

### 2.3 各页面右侧工作区

#### Home：经营总览

页面顺序固定为“指标卡 → 经营趋势 → 诊断摘要 → 决策摘要”：

1. 顶部 4 张卡片：Revenue/GMV、Orders、True ROAS、New Customers；没有广告花费或新客口径时明确显示“未接入/暂无数据”。
2. 主趋势卡：默认展示最近 30 天 Revenue/GMV，右上角展示数据源核对信息；支持切换 GMV、订单、转化率。
3. 下方左右两列：漏斗摘要与渠道归因摘要。
4. 页面底部：正在运行的实验及“继续观察/显著优于/存在风险”等状态。

#### Monitor：数据指标监测

- 顶部筛选：时间、粒度、实验组、渠道、设备、地区和商品。
- 第一行：DAU、GMV、Orders、Purchase Conversion、AOV、New Customers。
- 第二行：GMV/订单/转化率趋势，可在同一时间轴比较当前窗口和对比窗口。
- 第三行：异常指标列表，点击异常进入对应维度下钻。

#### Attribution：归因分析

- 顶部显示归因模型、回溯窗口和归因覆盖率。
- 第一行：Attributed GMV、Attributed Orders、Unknown Share、Top Channel。
- 主区域：渠道贡献排行与 GMV/订单贡献图。
- 下方：模型对比表和渠道 → 活动 → 触点 → 订单路径。

#### Funnel：漏斗诊断

- 顶部显示统一时间和人群筛选。
- 主区域使用横向漏斗图，旁边显示每步人数、相邻转化率和流失人数。
- 下方显示按渠道、设备和实验组拆分的漏斗表。
- 发现漏斗倒挂时，在图表旁显示“数据质量异常”，不把异常人数悄悄修正为递减。

#### Experiments：实验中心

- 列表页使用表格/卡片混合布局：实验名、状态、运行时间、主指标、样本量、uplift、健康度。
- 新建实验按钮放在页面标题右侧；创建表单采用分组区块，不使用全屏弹窗。
- 详情页顶部显示实验配置和状态，主体显示 A/B 指标卡、趋势、置信区间、SRM、护栏和决策文案。
- 实验详情始终比较完整 A/B 组；全局实验组筛选不覆盖详情中的统计比较。

#### Customers、Creatives、Integrations

- 这些导航先作为统一壳层中的可见入口，避免产品结构以后再次变化。
- 未实现能力显示明确的“规划中”状态、预计数据来源和依赖，不显示伪造的业务数据。
- Customers 在 M7 接入新老客/复购；Creatives 在 M8 接入 campaign；Integrations 在 M8 接入真实连接器。

### 2.4 前端技术实现

- 在当前 Streamlit 基础上使用 `st.navigation`/页面路由或等价状态路由实现模块切换；禁止在 `app.py` 中堆叠所有页面业务逻辑。
- 建议目录：

  ```text
  frontend/
  ├── app.py                 # 应用壳层、全局状态和导航
  ├── navigation.py          # 导航项、权限/可用状态和当前路由
  ├── theme.py               # 深色主题、颜色、间距和图表模板
  ├── api_client.py          # 后端请求封装
  ├── pages/
  │   ├── home.py
  │   ├── monitor.py
  │   ├── attribution.py
  │   ├── funnel.py
  │   ├── customers.py
  │   ├── experiments.py
  │   ├── creatives.py
  │   └── integrations.py
  └── components/            # metric_card、chart_card、status_badge、empty_state 等
  ```

- `app.py` 只负责设置页面、渲染左侧导航、注入全局筛选状态和调用当前页面；页面文件负责编排，组件负责展示，后端负责计算。
- 全局状态至少包括 `current_page`、`date_range`、`data_source`、`refresh_nonce`；页面局部状态不得覆盖实验详情的全量比较口径。
- 统一缓存策略：查询结果短 TTL 缓存；切换页面不重复请求未变化的参数；点击立即刷新通过 nonce 显式失效缓存。
- 主题样式集中定义，禁止在各页面散落颜色值、边距和 Plotly 配置。

### 2.5 视觉与交互验收标准

- 默认进入 Home，左侧导航在所有模块保持位置、宽度和顺序一致。
- 当前导航项有明确选中态；键盘和文本用户可以识别当前页面，不能只依赖颜色传递状态。
- 右侧内容区在 1280px 以上桌面宽度下使用宽布局；卡片之间有一致间距，图表不被裁切。
- 页面背景、导航背景、卡片背景和边框有清晰层级；正文与辅助文字满足可读对比度。
- 关键数值、更新时间、数据源、筛选条件和单位在首屏可见。
- 点击导航只更新右侧工作区；全局时间筛选在页面切换后保留。
- API 超时、空数据、样本不足和数据质量异常均显示在对应卡片内，不能出现空白区域或全页报错。
- 在窄屏下允许导航折叠为图标/抽屉，但不改变信息顺序；本期不追求移动端完整编辑体验。

---

## 3. 技术架构与实施原则

### 3.1 目标架构

```text
当前：数据模拟器 ───────────────┐
未来：Shopify / Ads / 埋点 SDK ─┼──> 数据接入与标准化
                                │
                                ▼
                        Supabase Postgres
                                │
                 ┌──────────────┼──────────────┐
                 ▼              ▼              ▼
             指标服务        归因服务        实验服务
                 └──────────────┼──────────────┘
                                ▼
                           FastAPI API
                                ▼
                      Streamlit + Plotly Web
```

### 3.2 实施原则

- **决策链路优先**：每个页面都应回答明确的业务问题，并提供下钻或下一步行动。
- **口径集中**：指标、归因和实验计算只在后端服务层定义；前端只格式化和展示。
- **透明可解释**：归因规则、统计方法、时间窗口、空值和异常状态必须可见。
- **模拟闭环先行**：先把产品体验与计算契约做完整，再接真实平台，避免外部授权阻塞 MVP。
- **数据质量内建**：事件顺序、漏斗倒挂、实验流量失衡、未知渠道和数据延迟不是静默异常。
- **可重复验收**：数据库初始化、历史造数、持续模拟、测试和启动都使用标准命令。
- **安全边界明确**：密钥仅在后端；所有 SQL 参数化；生产部署前补鉴权、审计和来源限制。

---

## 4. 当前基线（M0–M4，已完成）

| 里程碑 | 已有能力 | 当前实现 |
| --- | --- | --- |
| M0 工程基础 | 配置、标准命令、健康检查、测试骨架 | Python 3.12、uv、FastAPI、Streamlit、Ruff、Pytest |
| M1 数据底座 | 用户、事件、实验配置、历史数据和持续模拟 | Supabase Postgres、初始化/造数/模拟脚本 |
| M2 指标与漏斗 | DAU、GMV、订单数、转化率、AOV、趋势、固定三步漏斗 | `/api/metrics`、`/api/funnel` |
| M3 单实验评估 | A/B 指标、uplift、双比例 z 检验、业务结论 | `/api/experiment` |
| M4 经营看板 | 筛选、指标卡、趋势、漏斗、实验结果、自动刷新和模块级错误处理 | Streamlit + Plotly |

当前基线的限制：

- 页面仍是单页看板，没有三个业务模块的独立导航与下钻。
- `channel` 只有渠道级信息，缺少 source/medium/campaign 等营销层级。
- 单个固定实验 `homepage_checkout_v1`，没有实验生命周期、列表和结果沉淀。
- 只有 p-value 和最低样本阈值，没有置信区间、SRM、护栏指标和实验健康度。
- README 与原 PRD 仍使用“看板”定位，后续需统一更新为平台定位。

### 4.1 下一步前端重构（M4.1）

在增加归因和实验管理前，先把现有单页看板重构为统一应用壳层：

1. 建立固定左侧导航和右侧工作区，实现 Home、Monitor、Attribution、Funnel、Customers、Experiments、Creatives、Integrations 路由。
2. 将现有指标、趋势迁移到 Home/Monitor，将漏斗迁移到 Funnel，将单实验结果迁移到 Experiments。
3. 抽取深色主题、指标卡、图表卡、状态徽标、空状态和模块错误组件。
4. 未开发模块使用明确的规划状态页，不请求不存在的 API，也不展示模拟成真实的结果。
5. 保留现有全局时间范围、自动刷新、缓存和模块级失败隔离能力。

M4.1 完成后，新增业务模块只需增加页面与 API 客户端方法，不再改变整体导航和布局。

---

## 5. 数据模型演进

### 5.1 保留并扩展现有模型

| 对象 | 当前字段 | 下一阶段新增 | 用途 |
| --- | --- | --- | --- |
| `events` | 用户、会话、事件类型、组别、渠道、商品、金额、时间 | `experiment_id`、`source`、`medium`、`campaign_id`、`campaign_name`、`order_id`、`event_properties` | 指标、归因、实验和真实接入统一事实表 |
| `users` | 首次访问、获客渠道、国家、设备 | `customer_type`、可选外部用户标识 | 新老客和人群下钻 |
| `experiments` | 由现有 `experiment_config` 演进 | 名称、假设、状态、主指标、开始/结束时间、分流、最小样本、显著性水平 | 实验列表与生命周期 |
| `experiment_variants` | 当前 A/B 字段隐式表达 | 变体 ID、名称、是否对照、流量比例 | 支持每个实验独立配置变体 |
| `experiment_assignments` | 当前事件直接携带组别 | 实验、用户、变体、首次分配时间 | 保证同一用户稳定分组和样本口径 |
| `experiment_results` | 无 | 计算时间、窗口、指标快照、统计结果、决策和结论备注 | 结果沉淀与复盘 |
| `metric_thresholds` | 当前配置表中的两个阈值 | 指标、方向、阈值、作用范围、启用状态 | 统一预警配置 |

迁移时保留现有 API 的 `experiment_group` 兼容字段；新增实验服务优先读取 `experiment_assignments`，旧模拟数据可通过迁移脚本补入默认实验与对应分组。

### 5.2 统一事件与窗口约定

- 时间均为带时区 ISO 8601；数据库存 UTC；展示层明确用户时区。
- API 时间窗口使用左闭右开 `[start_time, end_time)`。
- 一条 `buy` 事件对应一笔订单，新增 `order_id` 后以其去重；缺失时兼容使用 `event_id`。
- 归因仅计算窗口内购买，并向前回溯 30 天触点；同一订单只使用购买发生前的触点。
- 无有效渠道的订单归入 `unknown`，不能因丢弃而导致总 GMV 不守恒。
- 比例均返回 `0~1`；零分母返回 `null`；金额使用数值类型，避免前后端字符串运算。

---

## 6. M5：归因分析闭环

### 6.1 目标

让投放和增长团队能够回答“订单与 GMV 来自哪些渠道、不同模型为何给出不同答案”。

### 6.2 归因模型

| 模型 | 规则 | 业务解释 |
| --- | --- | --- |
| `first_touch` | 将订单 100% 归给 30 天回溯窗口内最早有效触点 | 衡量拉新与首次影响 |
| `last_touch` | 将订单 100% 归给购买前最近一次有效触点 | 衡量临门促转贡献 |
| `linear` | 将订单和 GMV 平均分配给回溯窗口内所有有效触点 | 展示多触点共同影响 |

同一用户的重复连续同渠道事件先按“用户 + 会话 + 渠道 + 活动”折叠为一个触点，避免页面内多次点击人为放大贡献。线性模型的订单贡献允许为小数，展示时保留两位。

### 6.3 公共接口

`GET /api/attribution`

查询参数：`start_time`、`end_time`、`model=first_touch|last_touch|linear`，可选 `channel`、`campaign_id`。

响应至少包含：

- 时间窗口、30 天回溯窗口和模型标识。
- 总订单、总 GMV、已归因/未知订单与覆盖率。
- 渠道级 `order_credit`、`gmv_credit`、GMV 占比和排名。
- 有活动信息时返回 campaign 子级贡献。
- `data_quality`：未知渠道占比、缺少活动字段和无有效触点提示。

### 6.4 页面能力

- 独立“归因分析”页面，保留当前时间筛选并提供模型切换。
- 渠道 GMV/订单贡献图、排行表和归因覆盖率卡片。
- 并排显示三个模型的渠道贡献差异，突出变化最大的渠道。
- 支持从渠道下钻到活动，再查看订单级触点路径示例。
- 明确展示“归因代表规则下的贡献分配，不等同于因果增量”。

### 6.5 任务与验收

1. 扩展种子和持续模拟器，生成跨会话、多渠道、活动和订单标识数据。
2. 实现归因 schema、服务、路由和 API 客户端，所有模型复用统一触点抽取。
3. 新增归因页面组件与导航，不改变现有指标口径。
4. 覆盖单触点、多触点、重复触点、仅 buy、未知渠道、窗口边界和空数据测试。
5. 验收时三种模型均满足 GMV 守恒；渠道占比总和在数值精度内等于 1；切换模型后结果可解释地变化。

---

## 7. M6：实验中心

### 7.1 目标

将当前“固定实验结果卡片”升级为支持创建、运行监控、结果评估和结论沉淀的实验产品。

### 7.2 实验生命周期

`draft → running → paused → completed`，另支持从 `draft/running/paused` 进入 `cancelled`。

- `draft`：可编辑实验假设、指标、时间和分流。
- `running`：配置冻结，只允许暂停或结束；持续监测样本与健康度。
- `paused`：停止新增分配，保留已有数据；可恢复或结束。
- `completed/cancelled`：只读，允许补充结论与复盘备注。

### 7.3 实验配置

- 名称、业务假设、负责人和实验对象。
- 开始/结束时间、A/B 变体、分流比例与受众说明。
- 一个主指标：MVP 支持购买转化率。
- 辅助指标：GMV、AOV、加购率、订单数。
- 护栏指标：至少支持 GMV 或购买转化率的最大允许下降比例。
- 默认显著性水平 `0.05`、统计功效 `0.80`；最低样本量仍保留为演示安全阈值。

### 7.4 公共接口

| 接口 | 用途 |
| --- | --- |
| `GET /api/experiments` | 按状态、时间查询实验列表和摘要 |
| `POST /api/experiments` | 创建草稿并校验实验配置 |
| `GET /api/experiments/{id}` | 获取配置、运行健康度与最新结果 |
| `PATCH /api/experiments/{id}` | 仅在允许状态下更新配置或备注 |
| `POST /api/experiments/{id}/start` | 冻结配置并开始分配流量 |
| `POST /api/experiments/{id}/pause` | 暂停新增分配 |
| `POST /api/experiments/{id}/complete` | 结束实验并保存结果快照 |

现有 `GET /api/experiment` 在 M6 保持兼容，并返回默认实验结果；文档标记为 legacy，前端新页面改用资源型接口。

### 7.5 统计与健康度

- 继续使用双侧双比例 z 检验评估主指标，新增差值的 95% 置信区间。
- `uplift = (rate_B - rate_A) / rate_A`；A 组为 0 时返回 `null`。
- 决策优先级：数据异常 → 样本不足 → 护栏指标恶化 → 显著优于/低于 → 无显著差异。
- 增加 SRM（Sample Ratio Mismatch）检查；实际分流与配置显著不符时停止发布确定性结论。
- 展示累计趋势，但明确提示频繁查看 p-value 的局限；MVP 不实现序贯检验。
- 所有建议都是决策支持，不自动发布版本或调整流量。

### 7.6 页面与验收

- 实验列表展示状态、运行时间、样本量、主指标 uplift 和健康状态。
- 创建页使用分步表单；无效分流、结束早于开始、缺失主指标时禁止保存。
- 详情页包含配置摘要、A/B 指标、累计趋势、置信区间、SRM、护栏和结论。
- 完成实验时保存不可变结果快照，刷新页面不能因后续事件改变历史结论。
- 自动化测试覆盖合法状态迁移、非法修改、稳定分组、样本不足、SRM、护栏恶化和四种统计结果。

---

## 8. M7：指标监测、预警与数据质量

### 8.1 目标

让“总览”从静态展示升级为可发现问题、定位原因并跳转到归因或实验的监测入口。

### 8.2 指标扩展

- 保留 DAU、GMV、订单数、购买转化率和 AOV。
- 在数据可支持后增加新客数、新客占比和复购率。
- CAC、ROAS 仅在接入广告花费后启用；未接入时显示“缺少成本数据”，不得使用模拟为真实的结果。
- 指标卡返回当前值、对比窗口值、绝对变化、相对变化和数据状态。

### 8.3 监测与下钻

- 总览采用“核心指标 → 趋势 → 漏斗 → 归因摘要 → 实验摘要”的信息顺序。
- 支持渠道、设备、地区、商品、新老客和实验筛选；后端统一处理维度口径。
- 异常卡片提供下钻入口：渠道相关跳转归因，实验组相关跳转实验详情，漏斗变化跳转漏斗分析。
- 阈值预警在后端计算；返回 `normal/warning/critical/insufficient_data`，前端不写死规则。

### 8.4 数据质量

- 检测数据最新时间、事件延迟、漏斗倒挂、重复订单、未知渠道比例和实验 SRM。
- 每个模块独立返回质量状态，避免单个异常导致全页不可用。
- 质量异常必须区分“业务表现差”和“数据不可信”，禁止用同一种红色告警混淆。

### 8.5 验收

- 改变全局时间或维度筛选后，指标、趋势和漏斗使用一致窗口与口径。
- 对比值与独立 SQL/固定测试数据一致，跨日边界不重复计算。
- 模拟数据延迟、重复订单和漏斗倒挂时，页面显示正确质量提示。
- 用户能从一个异常指标在两次点击内进入对应的归因或实验详情。

---

## 9. M8：真实数据接入、部署与产品化准备

### 9.1 接入顺序

1. **Shopify**：订单、客户、商品和退款；先采用定时增量同步，再评估 Webhook。
2. **广告成本平台**：Google Ads、Meta Ads、TikTok Ads；统一 source/medium/campaign 和花费口径。
3. **站内行为 SDK/采集接口**：页面浏览、商品点击、加购、结算和购买事件。

每个连接器先落入原始表，再由标准化任务写入统一模型；保留外部 ID、同步批次、源更新时间和错误信息，避免直接把第三方响应当分析事实表。

### 9.2 接入能力

- 数据源连接状态、最近成功同步时间、延迟和错误摘要。
- 幂等增量同步、分页、限流重试和断点恢复。
- 渠道/活动映射与无法映射项的 `unknown` 兜底。
- Shopify 订单与站内购买事件去重；退款单独建模，不直接覆盖原订单。
- 模拟数据与真实数据使用明确的环境或项目隔离，页面始终标识数据来源。

### 9.3 部署与安全

- Supabase 保存标准化数据；数据库与 service role 密钥只注入后端环境。
- FastAPI 部署到 Render 或等价服务，健康检查 `/health`，结构化记录同步和查询错误。
- Streamlit 部署到 Community Cloud 或等价服务，只配置公开 API 地址。
- CORS 仅允许实际前端域名；写接口在公开部署前必须加入身份认证和操作审计。
- 数据库变更改用可追踪迁移；同步任务与 Web 服务分进程运行。

### 9.4 验收

- 同一批外部数据重复同步不产生重复订单或事件。
- 数据源异常可重试且不会留下半批次；恢复后从断点继续。
- 页面可区分模拟/真实数据，并展示数据新鲜度。
- 密钥不出现在仓库、前端、API 响应或日志中。

---

## 10. 跨阶段测试与文档

### 10.1 自动化测试

| 层级 | 覆盖范围 | 关键场景 |
| --- | --- | --- |
| 单元测试 | 指标、漏斗、触点折叠、三种归因、统计与决策 | 去重、窗口边界、GMV 守恒、零分母、SRM、护栏 |
| 数据测试 | 迁移、种子和模拟器 | 合法事件、订单唯一、跨渠道路径、稳定实验分组、可复现性 |
| API 契约测试 | 查询与写接口 | 正常、422、404、状态冲突、空数据、数据库不可用 |
| 前端测试 | API 客户端与关键组件 | 超时、错误、空值、数据质量状态和筛选参数 |
| 端到端验收 | 业务决策链路 | 异常发现 → 归因定位 → 实验判断 → 结果沉淀 |

每个里程碑合并前必须通过 `.venv/bin/lint` 和 `.venv/bin/test`。覆盖率以核心计算和状态迁移为重点，目标不低于 80%，但不得以无业务断言的测试凑行数。

### 10.2 文档同步

每个里程碑同时更新：

- `README.md`：当前能力、启动命令、API、数据来源和限制。
- `docs/data_dictionary.md`：字段、约束、指标与归因血缘。
- `docs/api_reference.md`：接口、状态码、请求/响应样例。
- `docs/demo_script.md`：5 分钟演示脚本，覆盖完整决策闭环。
- 原 PRD 或新版产品蓝图：统一使用“电商增长实验与营销归因平台”定位。

---

## 11. 推荐执行顺序与完成定义

| 顺序 | 里程碑 | 可独立演示的结果 | 完成定义 |
| --- | --- | --- | --- |
| 已完成 | M0–M4 基线 | 指标、漏斗、单实验结果看板 | 当前测试通过且 README 可复现启动 |
| 1 | M4.1 前端壳层 | 左侧模块导航和右侧独立工作区 | Home/Monitor/Funnel/Experiments 迁移完成，占位模块状态清晰 |
| 2 | M5 归因闭环 | 三种规则归因与渠道下钻 | GMV 守恒、模型切换可解释、测试通过 |
| 3 | M6 实验中心 | 创建、运行、评估、沉淀实验 | 生命周期、SRM、置信区间和结果快照可用 |
| 4 | M7 监测增强 | 异常发现并跳转诊断 | 对比、预警、质量状态和跨模块下钻可用 |
| 5 | M8 真实接入 | Shopify 与广告数据驱动分析 | 幂等同步、来源标识、安全和部署验收通过 |

### 下一实施切片：M4.1 前端应用壳层

首个切片只完成界面骨架与已有能力迁移，不同时新增业务 API：

1. 建立导航、页面路由、主题和右侧工作区容器。
2. 将现有指标卡与趋势迁移至 Home/Monitor，漏斗迁移至 Funnel，实验评估迁移至 Experiments。
3. 为 Attribution、Customers、Creatives、Integrations 建立不请求数据的规划状态页。
4. 验证全局筛选跨页面保留、导航选中态、模块错误隔离和 1280px 桌面布局。
5. 完成壳层后进入 M5.1：扩展多触点数据并实现三种归因 API。

整个项目的最终验收不是“页面齐全”，而是用户可以用同一套数据完成：

> 发现指标异常 → 通过漏斗和归因定位原因 → 查看或运行实验验证策略 → 获得可解释的上线或预算建议。
